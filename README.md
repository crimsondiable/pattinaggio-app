# Blading Manager Big Ball of Mud

Gestionale statico per test remoto via GitHub Pages.

La parte pubblicata e' in `3-gestionale/src`.

## Deploy

Il deploy e' gestito da GitHub Actions con il workflow:

`.github/workflows/deploy-pages.yml`

Il workflow pubblica solo `3-gestionale/src`.
